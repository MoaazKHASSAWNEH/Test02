{% extends "base.html.twig" %}

{% block title %} Articles {% endblock %}

{% block body %}
{% include "menu_user.html.twig" %}
<h1>Articles index</h1>
<p><a href="{{ path('article_new') }}" style="display:block;background-color: green;width=80%;margin:10px auto;padding: 10px;text-align:center;text-decoration:none">Créer un nouvel article</a></p>
<table>
    <tr>
        {# <th>ID</th> #}
        <th>TITRE</th>
        <th>RESUME</th>
        {# <th>DATE</th> #}
    </tr>
    
    {% for article in articles %}
    <tr>
        
        {# <td> {{ article.id }} </td> #}
        <td><a href="{{ path('article_id', {'id':article.id} ) }}">{{ article.titre }} </a></td>
        <td> {{ article.resume }} </td>
        {# <td> {{ article.date | date("d/m/Y") }} </td> #}
        
    </tr>
    
    {% endfor %}
    
</table>
{% endblock %}